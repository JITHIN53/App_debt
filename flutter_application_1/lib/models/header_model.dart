import 'dart:ui';

class HeaderModel {
  String image;
  String title;
  String subtitle;
  String count;
  Color color;
  HeaderModel(this.image, this.title, this.subtitle, this.count, this.color);
}
